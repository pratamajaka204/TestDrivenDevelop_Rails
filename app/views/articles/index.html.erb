<!DOCTYPE html>
<html>
<head>
   <title>List Data</title>
</head>
<body>

<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
		<ul class="nav navbar-nav">
			<li class="nav-item"><%= link_to 'Home', articles_path, class: 'btn btn-lg btn-primary' %></li>
			<li class="nav-item"><%= link_to 'Add Data', new_article_path, class: 'btn btn-lg btn-warning' %></li>
			<li class="nav-item"><%= link_to 'User', sign_up_path, class: 'btn btn-lg btn-info' %></li>
			<li class="nav-item">
					<%= link_to 'Logout' , new_session_path, class: 'btn btn-primary ml-2' %>
					
			</li>
			
		</ul>
</nav>
<div class="container">
	<div class="content-wrapper">
		<section class="content">
			<div class="row">
				<div class="col-md-12">
					<!-- CONTENT HERE -->
				      <%= yield :list %>
				      <!-- END OF CONTENT -->

					 <h2>Selamat Datang</h2>
					 <h5>
						<% if ! current_user %>
							<%= link_to 'Log-in', new_session_path %>
							<% else %>
							Hello <%= current_user.username %>
							<%= link_to 'Logout', session_path(current_user.id), class: 'btn btn-md btn-danger float-md-right', method: "DELETE" %>
							<% end %>
					</h5>

				      <div class="jumbotron">
				      	<h1 class="display-4">Hello, world!</h1>
						<p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
						<hr class="my-4">
						<p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
						<p class="lead">
						<a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
						</p>
				      </div>

				      <!-- Data Artikel -->
				      <table class="table table-striped">
				      	<thead>
				      		<tr>
				      			<th>Title</th>
				      			<th>Content</th>
				      			<th>Status</th>
				      			<th>Action</th>
				      		</tr>
				      	</thead>
				      	<tbody>
				      		<% @article.each do |article| %>
				      		<tr>
				      			<td><%= article.title %></td>
				      			<td><%= article.content.truncate(80) %></td>
				      			<td><%= article.status %></td>
				      			<td><%= link_to 'Read More', article_path(article.id) %>

				      				<%= link_to 'Edit', edit_article_path(article.id) %>
				      				<%= link_to 'Delete', article_path(article.id), method: "Delete", data: {confirm: 'Delete this data ? '} %>
				      				
				      			</td>
	   				       <% end %>
				      		</tr>
				      	</tbody>
				      </table>
				      
				      	
				     
				</div>
			</div>
		</section>
	</div>
</div>
	
  
</body>
</html>
